name: Deploy Hexo Blog
on: 
  push:
    branches:
      - main

jobs:
  test:
    name: Test Hexo Blog
    runs-on: ubuntu-latest

    steps:
      - name: hello hexo
        run: echo "hello hexo"
        
      - name: Checkout
        uses: actions/checkout@v6.0.2
      
      - name: 查看目录
        run: ls -lha


        